import React from 'react';

const loading = () => {
    return (
        <div className='container mx-auto'>
          <span className="loading loading-spinner text-neutral bg-white"></span>
        </div>
    );
};

export default loading;